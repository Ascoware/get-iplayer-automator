// Shared by the main app and the Safari extension so CFBundleShortVersionString
// stays in sync. Edited by bump_build.sh and release.sh — keep this format
// (one setting per line, no trailing inline comments) so the sed scripts work.

MARKETING_VERSION = 1.29.4
CURRENT_PROJECT_VERSION = 202605051609
